# 双人协作边界

本文档用于直接完整实现 [智能旅行助手.md](./智能旅行助手.md)。

- 配套附件：
  - [A_开工清单.md](./A_开工清单.md)
  - [B_首批交付清单.md](./B_首批交付清单.md)
- 本文档不是 `V0` 阶段说明，而是完整产品的双人分工边界。
- 分工原则：`A` 负责更独立的前端消费层；`B` 负责规划内核、共享 schema、外部服务与复杂集成。

## 完整产品范围

以下能力都必须有人负责，不能再视为后续补充：

- 智能行程规划
- 地图可视化
- 预算计算
- 行程编辑
- PDF / 图片导出
- 景点图片补全
- 加载进度与状态提示
- 侧边导航与锚点跳转

## 分工

### A 负责：前端消费层与结果页体验

- 负责目录：`frontend/`
- 主要职责：
  - 规划输入页与结果页交互
  - 地图、预算、天气、每日行程展示
  - 局部编辑按钮、交互反馈与前端状态处理
  - 加载进度条、状态提示、错误态与空态
  - 侧边导航、锚点跳转、结果页信息架构
  - 导出入口与前端导出流程编排
  - 前端接口调用封装与前端类型同步
- 主要文件：
  - `frontend/src/views/`
  - `frontend/src/services/api.ts`
  - `frontend/src/types/index.ts`
  - `frontend/tests/services/`
  - `frontend/tests/views/`

### B 负责：规划内核、共享契约与复杂集成

- 负责目录：`trip_planner/` 与 `tests/`
- 主要职责：
  - `TripPlanRequest`、`TripPlan`、`EditRequest` 及其嵌套模型的字段定义与稳定性
  - FastAPI 接口与 `OpenAPI/Swagger`
  - LangGraph 工作流、多 agent 编排、服务接入、外部依赖适配
  - 景点图片补全、图片 URL 写回、结果后处理
  - 导出所需的后端支持能力，如静态地图、导出数据整形或服务端截图方案
  - 后端契约测试、工作流测试、服务边界测试
- 主要文件：
  - `trip_planner/api.py`
  - `trip_planner/workflow.py`
  - `trip_planner/models/`
  - `tests/test_api.py`
  - `tests/test_workflow.py`
  - `tests/test_service_boundary.py`

## 共享边界

- `B` 决定后端返回什么：字段名、必填项、字面量取值、导出所需数据、编辑接口参数。
- `A` 决定这些数据在前端如何展示、排序、交互、导出和反馈。
- 顶层共享 contract 至少包括：
  - `TripPlanRequest`
  - `TripPlan`
  - `EditRequest`
- `TripPlan` 之下的嵌套模型同样属于共享 contract 的一部分，但归 `B` 主导定义，`A` 负责镜像消费与前端类型同步。

## Swagger 的使用边界

- 使用 `OpenAPI/Swagger` 作为后端 HTTP 接口真相。
- `Swagger` 用于约定请求、响应、字段名、必填项和字面量取值。
- 不使用 `Swagger` 描述前端页面布局、前端派生状态、前端交互细节或后端内部工作流节点。
- 若共享字段要改，先由 `B` 更新 schema 与接口，再通知 `A` 同步前端类型、页面消费和导出逻辑。

## 完整产品中的专项归属

### 导出功能

- `A` 负责：
  - 导出按钮、导出状态提示、导出前隐藏/调整前端视图
  - 图片导出与 PDF 导出在前端的触发流程
- `B` 负责：
  - 地图导出兼容性方案
  - 静态地图、服务端截图、导出数据整形等复杂支持能力

### 景点图片补全

- `B` 负责图片来源接入、图片 URL 写回和结果数据稳定性。
- `A` 负责图片展示、降级占位和加载失败时的前端容错。

### 结果页增强体验

- `A` 负责加载进度、状态提示、侧边导航、锚点跳转和结果页排版。
- `B` 负责提供足够稳定的阶段性状态信息或结果数据，供 `A` 消费。

## 联调规则

- `B` 需要提供：
  - 一份可访问的 `OpenAPI`
  - 2 到 3 个稳定的 `TripPlan` 示例 JSON
  - 若导出或图片补全需要额外字段，也应在示例中体现
- `A` 在真实服务未完成前，可先基于示例 JSON 或 mock 开发页面。
- `A` 不直接改后端 schema；若字段不够用，向 `B` 提出 contract 变更需求。
- `B` 改动共享字段后，应同时检查后端测试、前端类型、结果页展示与导出影响。

## 实施顺序

1. `B` 先冻结第一版 `TripPlanRequest`、`TripPlan`、`EditRequest` 与关键嵌套模型。
2. `B` 产出 `OpenAPI`、示例 JSON，并明确图片补全与导出所需字段。
3. `A` 基于固定 contract 完成结果页、地图、预算、局部编辑、进度提示和侧边导航。
4. `B` 继续完善真实服务接入、图片补全与导出支撑能力。
5. `A` 接入真实接口，完成导出入口、图片展示与最终交互收口。
6. 双方以完整产品能力为验收，而不是只以 `V0` 闭环为验收。
