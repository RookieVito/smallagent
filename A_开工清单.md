# A 开工清单

本文档给 `A` 使用。默认 `A` 负责前端消费层与结果页体验。

## 你现在就可以开始做

- 规划输入页提交流程
- 结果页基础布局
- 地图展示区块
- 预算区块
- 天气区块
- 每日行程区块
- 局部编辑按钮与交互反馈
- 加载进度条与状态提示
- 侧边导航与锚点跳转
- 导出按钮与前端导出流程入口

## 你主要改这些位置

- `frontend/src/views/`
- `frontend/src/services/api.ts`
- `frontend/src/types/index.ts`
- `frontend/tests/services/`
- `frontend/tests/views/`

## 你开始前需要从 B 拿到的东西

- 一份可访问的 `OpenAPI/Swagger`
- 2 到 3 个稳定的 `TripPlan` 示例 JSON
- `TripPlanRequest`、`TripPlan`、`EditRequest` 的第一版字段定义
- 若图片补全或导出要用额外字段，B 需要一开始就说清楚

## 你可以先不用等真实服务完成

- 你可以先用 mock 或示例 JSON 做页面
- 你可以先把 `frontend/src/types/index.ts` 对齐第一版 contract
- 你可以先把结果页和编辑交互做顺，再切真实接口

## 你不要负责这些

- 后端 schema 设计
- FastAPI 接口定义
- LangGraph 工作流
- 外部服务接入
- 图片 URL 回填逻辑
- 地图导出兼容性方案
- 服务端截图或静态地图方案

## 你每天自检这几件事

- 有没有直接依赖一个尚未冻结的后端字段
- 页面展示是否只消费 contract，不偷偷定义后端语义
- 编辑交互是否只改前端消费逻辑，不私自改 schema
- 导出入口是否和结果页结构解耦，避免后面难改
- 改动后是否同步更新前端测试

## 当你需要找 B 时

- 字段不够用
- 字段含义不清楚
- 某个字段是否必填不明确
- 导出需要额外数据
- 图片展示需要后端补字段
- 编辑操作需要新增接口参数

## 你的第一轮完成标准

- 输入页能发起请求
- 结果页能基于示例 JSON 完整展示
- 地图、预算、天气、每日行程都能正确渲染
- 局部编辑交互可用
- 加载进度和状态提示可用
- 侧边导航与锚点跳转可用
- 导出入口已预留，前端流程已接好
